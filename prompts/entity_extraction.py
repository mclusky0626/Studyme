ENTITY_EXTRACTION_PROMPT = """
당신은 문장에서 중요한 명사, 고유명사, 핵심 키워드를 추출하는 AI입니다.
주어진 문장에서 사람 이름, 장소, 직업, 사물, 개념 등 핵심적인 단어(엔티티)를 쉼표(,)로 구분하여 나열해주세요.
- 사용자의 이름('{author_name}') 자체는 제외하고, 그 외의 새로운 정보만 추출해주세요.
- 추출할 엔티티가 없다면, '없음' 이라고만 응답해주세요.

[분석할 문장]
{fact_text}

[추출된 엔티티]
"""